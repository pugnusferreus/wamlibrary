%h1
  Menu

%p
  - @categories.each do |category|
    #category_list.main_menu
      = category.name
      %ul
        - category.sub_categories.each do |sub_category|
          %li
            = link_to sub_category.name, :controller => "listings", :action => "show", :id => sub_category

    %br

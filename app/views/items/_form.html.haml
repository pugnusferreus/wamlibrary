= javascript_include_tag 'page_related/items'

.field
  = form.label :name
  *
  %br
  .text_box_wrapper
    = form.text_field :name, :class => "required text_box"

.field
  = form.label :author
  *
  %br
  .text_box_wrapper
    = form.text_field :author, :class => "required text_box"

.field
  = form.label :description
  %br
  .text_area_wrapper
    = form.text_area :description, :class => "text_area"

.field
  = form.label :sub_category
  *
  %br
  - sub_categories = SubCategory.all
  - sub_categories.sort!  {|a,b| a.name<=> b.name}
  = collection_select(:item, :sub_category_id, sub_categories, :id, :name, {:prompt => "Please select a sub category"}, :class => "required")

.field
  = form.label :active
  %br
  %input{:type=>"checkbox",:id=>"active",:name=>"item[active]",:value=> "true",:checked=>@item.active}

.field
  = form.label :year
  %br
  .text_box_wrapper
    = form.text_field :year, :class => "number text_box"

.field
  Index Number
  *
  %br
  .text_box_wrapper
    = form.text_field :index_num, :class => "required text_box"

.field
  = form.label :contents
  %br
  .text_box_wrapper
    = form.text_field :contents, :class => "text_box"

%h1
  Edit User Details

- form_for :user_maintenance, @user, :url => { :action => "update" }, :html => { :method => :put } do |f|
  
  - if @user.errors.any?
    #error_explanation
      %h2
        = pluralize(@user.errors.count, "error")
        prohibited this post from being saved:

      %ul
        - @user.errors.full_messages.each do |msg|
          %li
            = msg

  = render :partial => "form", :object => f

  %p
    = f.submit 'Update'
    
    

      
%p
  = link_to 'Show', user_maintenance_path
  |
  = link_to 'Back', user_maintenance_index_path


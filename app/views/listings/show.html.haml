%h1
  #{SubCategory.find(@list.sub_category_id).category.name} - #{SubCategory.find(@list.sub_category_id).name}

%input{ :type => "hidden", :id => "sub_category_id", :value =>"#{@list.sub_category_id}"}
%table{:id => "list_table", :class => "display"}
  %thead
    %tr
      %th
        Name
      %th
        Author
      %th
        Status
      %th
        Description
      %th
        Category
      %th
        &nbsp;
  %tbody
    %tr
      %td{ :colspan => "6" }
        Loading data from server
  %tfoot
    %tr
      %td{ :colspan => "6"}
%br
